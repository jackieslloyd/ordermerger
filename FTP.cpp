#include "FTP.h"

#include <windows.h>
#include <string.h>
#include <wininet.h>
#include <iostream>
#pragma comment(lib, "Wininet")

using namespace std;

int FTP::Upload()
{
    // testing github1
    //string s("eee");
    //LPCWSTR sc = s.c_str;
    ////LPCTSTR lpszTest = _T("Test String");
    ////LPCWSTR f = "www";
    ////string s("eee");
    //HINTERNET hInternet = InternetOpen(NULL, INTERNET_OPEN_TYPE_DIRECT, NULL, NULL, 0);
    //HINTERNET hFtpSession = InternetConnect(hInternet, L"HOST", INTERNET_DEFAULT_FTP_PORT, L"USER", L"PASS", INTERNET_SERVICE_FTP, INTERNET_FLAG_PASSIVE, 0);
    //FtpPutFile(hFtpSession, L"C:/test.txt", L"/test.txt", FTP_TRANSFER_TYPE_BINARY, 0);
    //FtpPutFile(hFtpSession, sc, L"/test.txt", FTP_TRANSFER_TYPE_BINARY, 0);
    //cout << "File Uploaded." << endl;
    //InternetCloseHandle(hFtpSession);
    //InternetCloseHandle(hInternet);
    return 0;
}

